/* KasirPro AT-08 — Management Dialog proxy ke dialog global */
import { showDialog } from '../app-dialog.js';
export { showDialog };
export function installManagementAlertBridge(){ /* retired AT-08: global dialog already active */ }
